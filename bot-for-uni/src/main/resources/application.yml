telegram:
  bot:
    username: ${BOT_NAME}
    token: ${BOT_TOKEN}

spring:
  dotenv:
    enabled: true
  datasource:
    url: ${DATASOURCE_URL}
    username: ${DATASOURCE_USERNAME}
    password: ${DATASOURCE_PASSWORD}


link:
  address: 127.0.0.1:8086

salt: ${SALT}


#  jpa:
#    hibernate:
#      ddl-auto: update
